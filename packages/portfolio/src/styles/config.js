import { rgba } from 'emotion-rgba';
import colors from "./colors";

export default {
  containerWidth: "1200px",
  postHeight: "60vh",
  // for all lines
  lineHeight: "4px",
  gradient: `linear-gradient(-30deg, ${rgba(colors.secondary, 1)} 0%, ${rgba(colors.primary, 1)} 100%)`
}